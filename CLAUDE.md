# 山川園芸 公式ECサイト — プロジェクト規約

鹿児島県指宿市山川のライチ農園「山川園芸」のECサイト。
ルートの `c:\projects\CLAUDE.md`（Elite Web Agency Master System）に加えて、
このプロジェクトでは以下を守る。

---

## 最優先ルール：確認できないことを書かない

このサイトは、生産者ご本人から提供された情報と、
公式Instagram・公式オンラインショップ（BASE）で公開されている内容だけで
作られている。それ以外の事実を書き足さない。

**特に、以下は絶対に創作しない。**

- ライチの品種・糖度・果実サイズ
- 栽培方法・栽培年数・農薬について・生産量
- 送料・配送方法・温度帯（「冷蔵便」等と書かない）・配送地域
- ギフト包装・のし・メッセージカードの対応可否
- 発送日指定の可否
- 農園での直売・見学の可否
- 代表者の経歴・受賞歴・農園の歴史
- レビュー・評価（`review` / `aggregateRating` を構造化データに出さない）

### 未確認をどう扱うか

データ側で `null`（または空文字）にする。それだけでよい。

- 画面からその行が消えるか、「確認中です」と表示される
- 構造化データからも自動的に除外される
- FAQ は質問だけを出し、回答欄には問い合わせ導線が出る

推測で埋めるくらいなら、空のままにしておくこと。

### 出典コメントを残す

データファイルの各項目には、値の出どころをコメントで書く。

```ts
volume: "500g", // [公式]
saleStart: null, // [TODO] 今季の販売開始日
```

| 記号 | 意味 |
| --- | --- |
| `[確認済]` | ご本人から提供された情報 |
| `[公式]` | 公式Instagram / 公式オンラインショップの掲載内容 |
| `[TODO]` | 未確認。要ヒアリング |

---

## ライチ完全ガイド（`/lychee`）のルール

このサイトは「ライチ専門メディア＋生産農家のEC」を目指している。
`/lychee` 配下がその中核。

- **1キーワード＝1ページの量産をしない。** 同じ検索意図は1ページに統合する。
  ページを足す前に `src/data/lycheeGuide.ts` の `intent` 欄を必ず確認する
- ページの metadata・パンくず・Article構造化データ・関連リンク・sitemapは
  `lycheeGuide.ts` と `GuideLayout` が自動で作る。手で書き足さない
- 各ページの冒頭に `AnswerBox` で**最短回答**を置く。2〜3文で言い切る
  （検索してきた人が10秒で答えを得られること。AI検索にも拾われる）
- ピラーページ（`/lychee`）で各テーマを書き切らない。案内役に徹する
- 記事から商品へは `ShopCta`、商品から記事へは商品ページの内部リンクで
  **双方向**につなぐ。片方向にしない
- アンカーテキストを「詳しくはこちら」にしない。
  「ライチの保存方法と日持ちを見る」のように、リンク先が分かる文言にする

### 栄養・健康の記述（YMYL）

- 数値は `src/data/nutrition.ts` からのみ読む。本文に数字をベタ書きしない
- 出典は文部科学省の食品成分データベース。
  **AIの記憶や一般ブログを根拠にしない。** 成分表が改訂されたら実際に確認する
- 効果を断定しない。禁止例:
  「美肌になる」「免疫力が上がる」「貧血が治る」「病気を予防する」「ダイエットできる」
- 「○○の働きに関わる栄養素です」という書き方に留める
- 数値・統計を載せたページには必ず `SourceList` で参考資料を表示する

### 農園の一次情報

まとめサイトに勝てる唯一の要素。`FarmNote` コンポーネントで差し込む。
**伺っていない内容を生産者の言葉として書かないこと。**
確認が取れた記事だけ `lycheeGuide.ts` の `reviewedAt` に日付を入れると
「山川園芸確認済み」が表示される。確認していない記事に入れない。

---

## データ層を通す

商品・FAQ・お知らせ・ストーリー・コラムを JSX に直接書かない。
必ず `src/data/` を更新する。1箇所の更新で
一覧・詳細・構造化データ・sitemap・商品フィードがすべて揃う。

| ファイル | 内容 |
| --- | --- |
| `src/data/siteConfig.ts` | 屋号・連絡先・販売状況・送料・決済・ナビ |
| `src/data/products.ts` | 商品 |
| `src/data/faq.ts` | よくある質問 |
| `src/data/news.ts` | お知らせ |
| `src/data/story.ts` | 農園のストーリー（/about） |
| `src/data/column.ts` | 農園の記録（コラム） |
| `src/data/lycheeGuide.ts` | ライチ完全ガイドのページ登録簿 |
| `src/data/nutrition.ts` | 栄養成分（出典つき） |

---

## 売るための設計

最優先KPIは「ライチを売り切ること」。ただし煽らない。

- **禁止**：「残り○個」「今すぐ買わないと損」など、実データにない在庫の煽り
- 希少性は事実だけで伝える（旬が短い／収穫量に限りがある／産地から直送）
- 価格を出すときは必ず「税込／送料別」を添える
- 送料が未確定のあいだ、合計金額に架空の送料を足さない
- 売り切れ・販売準備中・価格未確定の商品はカートに入れられない
- スマホを最優先。375px で商品・価格・数量・購入ボタンが快適に扱えること

---

## デザイン

- 白・オフホワイトが基調。メインは深いグリーン、アクセントはライチの赤〜ピンク
- **赤は購入導線にだけ使う**。赤と緑を同じ面積で使わない
- 角丸カードを大量に並べない。同じ大きさのカードの3列・4列グリッドを繰り返さない
- 写真は大きく。全幅・画面半分・縦長を使い分け、余白を大胆に取る
- 写真と文章が交互に現れる、雑誌のような組みを基本にする
- 意味のないアイコン・過剰な影・不自然なグラデーションを使わない
- 見出しは明朝体（`font-mincho`）、本文はゴシック（`font-gothic`）

### コピー

短く、具体的に、農家の温度を残す。

- OK: 「今年も、指宿のライチが実りました。」「木になった実を、旬の時期に。」
- NG: 「最高品質の革新的な」「こだわり抜いた究極の」「唯一無二の体験」

---

## 技術上の約束

- Tailwind v4。独自クラスは必ず `@layer components` に置く
  （レイヤー外に書くと `position` 等がユーティリティを打ち消してレイアウトが壊れる）
- `.reveal` の非表示ルールは `.js` 配下に限定する
  （JSが動かない環境で本文が消えないため）
- `Photo` コンポーネントに `absolute` を渡さない。`position: relative` の枠を作るため、
  背景として敷き詰める用途には `next/image` を直接使う
- 決済の金額は必ずサーバー側で `products.ts` から引き直す。
  クライアントから送られた価格を信用しない
- APIキーをコードに書かない。必ず環境変数から読む
- 本番ドメインは `src/data/siteConfig.ts` の `PRODUCTION_URL` に一元化されている。
  canonical・OGP・sitemap・構造化データ・商品フィードがすべてこの値を使うので、
  URLをページ側にベタ書きしない
- 変更後は `npm run build` `npm run lint` `npm run typecheck` をすべて通す
